// Declaring variable.
let a:number=10;
// a="bharat";  error

// Declaring Array.

let arr:number[]=[1,2,3];

// Declaring functions.

const add = (a:number,b:number):number => {
return a+b;
}

add(10,5);
// add(10,"bharat");    error
console.log(add(10,5));

